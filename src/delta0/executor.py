"""Executor — applies `Action`s to the venues. M0: placeholder.

Wired in M3 (LIVE_SMALL). Pre-M3, the bot never executes anything; it only
observes. See README sections 3 and 14 for the mode gates.
"""

from __future__ import annotations


class Executor:
    def __init__(self) -> None:
        raise NotImplementedError(
            "Executor will be implemented in M3. See README section 3.",
        )
